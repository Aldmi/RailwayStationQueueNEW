<Window x:Class="TerminalUIWpf.Views.AppView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        AllowDrop="False"
        Focusable="False"
        WindowState="Maximized"
        WindowStyle="None"
        mc:Ignorable="d">


    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="65" />
            <Setter Property="Background" Value="{Binding Path=ColorBtn, Mode=OneWay}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="1,15,1,15" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="IsEnabled" Value="{Binding Path=BtnEnable, Mode=OneWay}" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="MaxWidth" Value="1500" />
        </Style>

        <Style x:Key="TextBlockMainStyle" TargetType="{x:Type TextBlock}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TextBlockErrorStyle" TargetType="{x:Type TextBlock}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="0,0,0,45"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Background="Crimson">
            
            <TextBlock MaxWidth="1000"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       FontSize="100"
                       Style="{StaticResource TextBlockMainStyle}"
                       Foreground="White"
                       Padding="5,50,5,5"              
                       Text="Выберите специалиста"
                       TextAlignment="Center"
                       TextWrapping="Wrap" IsEnabled="{Binding Path=IsConnect, Mode=OneWay}" />

            <TextBlock MaxWidth="1000"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       FontSize="70"
                       Style="{StaticResource TextBlockErrorStyle}"
                       Foreground="White"
                       Padding="5,90,5,5"
                       Text="ОТСУТСТВУЕТ СВЯЗЬ. Обратитесь пожалуйста к администратору."
                       TextAlignment="Center"
                       TextWrapping="Wrap" IsEnabled="{Binding Path=IsConnect, Mode=OneWay}"   />
        </Grid>


        
        <Button Name="BtnGetHelp"
                Grid.Row="1"
                Content="Получить справку"      
                Style="{StaticResource ButtonStyle}" />

        <Button Name="BtnBuyTicket"
                Grid.Row="2"
                Content="Купить билет на поезд"      
                Style="{StaticResource ButtonStyle}" />

        <Button Name="BtnBuyInterstateTicket"
                Grid.Row="3"
                Content="Купить билет на поезд меж государственного сообщения"      
                Style="{StaticResource ButtonStyle}" />

        <Button Name="BtnBaggageCheckout"
                Grid.Row="4"
                Content="Хранение багажа"      
                Style="{StaticResource ButtonStyle}" />

        <Button Name="BtnAdmin"
                Grid.Row="5"
                Content="Администратор"      
                Style="{StaticResource ButtonStyle}" />

    </Grid>
</Window>
